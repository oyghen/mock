[project]
name = "mockcli"
version = "0.1.0"
description = "mock cli"
readme = "README.md"
authors = [
    { name = "oyghen", email = "oyghen@duck.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mockcli = "mockcli:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
